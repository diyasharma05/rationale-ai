# Test-only dependencies. Production deps live in requirements.txt.
pytest>=8,<10
ruff>=0.6,<1
# the service wrapper (ops/bench.py and tests/integration/test_api.py);
# the Dockerfile installs these into the api image explicitly
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
httpx>=0.27,<1
